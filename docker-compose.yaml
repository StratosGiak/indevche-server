services:
  server:
    build: ./nginx
    image: stratosgiak/indevche-nginx
    restart: always
    ports:
      - 80:80
      - 443:443
    depends_on:
      - node_app
  node_app:
    init: true
    restart: always
    build: .
    image: stratosgiak/indevche-node
    environment:
      NODE_ENV: production
    env_file:
      - .env
    ports:
      - 3000:3000

volumes:
  shared_volume:
    external: true
