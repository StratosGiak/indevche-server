services:
  indevche:
    init: true
    image: stratosgiak/indevche
    build:
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    env_file:
      - .env
    ports:
      - 3000:3000
